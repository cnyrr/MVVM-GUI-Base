<Application x:Class="MVVM_Base.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MVVM_Base">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>

                <!--
                    Merge order matters.

                    1. Sizing — theme-independent dimensional tokens (spacing,
                       font sizes, hit targets, shell geometry).
                    2. Tokens — theme-token KEY declarations with magenta
                       placeholder values.
                    3. (Theme dictionary is appended at runtime by ITheming
                       AFTER Tokens, so its values override the placeholders.)
                    4. Components — control styles. Reference both Sizing and
                       theme tokens via DynamicResource. Added in step 5.
                    5. DataTemplates — VM ↔ View mappings.
                -->
                <ResourceDictionary Source="Themes/Sizing.xaml" />
                <ResourceDictionary Source="Themes/Tokens.xaml" />
                <ResourceDictionary Source="Themes/Components/Buttons.xaml" />
                <ResourceDictionary Source="Resources/DataTemplates.xaml" />
                
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
