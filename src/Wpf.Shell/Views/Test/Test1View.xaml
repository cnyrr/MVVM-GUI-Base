<UserControl x:Class="Wpf.Shell.Views.Test.Test1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Wpf.Shell.ViewModels.Test"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920"
             Background="{DynamicResource Brush.Surface.Content}">

    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MaxWidth="{DynamicResource Width.Content.Narrow}">

            <TextBlock Text="Test 1"
                       HorizontalAlignment="Center"
                       FontFamily="{DynamicResource FontFamily.Default}"
                       FontSize="{DynamicResource FontSize.Display}"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource Brush.Content.Primary}" />

            <TextBlock HorizontalAlignment="Center"
                       Margin="0,16,0,0"
                       FontFamily="{DynamicResource FontFamily.Default}"
                       FontSize="{DynamicResource FontSize.BodyLarge}"
                       Foreground="{DynamicResource Brush.Content.Secondary}">
                <Run Text="Visited" />
                <Run Text="{Binding VisitCount}" />
                <Run Text="time(s)." />
            </TextBlock>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,32,0,0">
                <Button Command="{Binding GoBackCommand}"
                        Content="Back"
                        Padding="16,8" />
                <Button Command="{Binding GoForwardToTest2Command}"
                        Content="Forward to Test 2"
                        Padding="16,8"
                        Margin="12,0,0,0" />
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>